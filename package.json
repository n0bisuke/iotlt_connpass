{
  "name": "connpass_auto_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node action.js -exec",
    "server": "node server.js",
    "github": "git add -A && git commit -m 'update' && git push origin master",
    "deploy": "git add -A && git commit -m 'update' && git push webapp master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.4",
    "google-auth-library": "^0.10.0",
    "googleapis": "^21.3.0",
    "puppeteer": "^0.10.1",
    "socket.io-client": "^2.0.3"
  },
  "engines": {
    "node": ">=8.1.0"
  }
}
